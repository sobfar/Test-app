{
  "name": "vazhebaf",
  "version": "1.4.0",
  "description": "واژه‌باف - بازی کلمه‌سازی فارسی",
  "scripts": {
    "sync": "npx cap sync android",
    "generate:icons": "npx capacitor-assets generate --android --assetPath resources"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/assets": "^3.0.0"
  }
}
