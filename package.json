{
  "name": "board-setup-app",
  "version": "1.0.0",
  "description": "اپ اتصال و تنظیم برد از طریق وای‌فای",
  "scripts": {
    "build": "node scripts/build.js",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capgo/capacitor-wifi": "^8.0.0",
    "@capgo/capacitor-inappbrowser": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "esbuild": "^0.24.0"
  }
}
